import * as mapboxgl from 'mapbox-gl';
export interface PointM {
  type:string,
  coordinates: [
    number,number
  ]
  }

export interface PointDetay  {
  type:string,
  coordinates:[[number,number]]
}
